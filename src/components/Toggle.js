const Toggle = () => {
    return (
        <label className="switch">
            <input type="checkbox" id = "checkbox"/>
            <span id="slider"></span>
        </label>
    )
}

export default Toggle
